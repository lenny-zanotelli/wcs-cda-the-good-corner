export type CategoryProps = {
  id: number;
  name: string;
};

export type AdCardProps = {
  id: number;
  title: string;
  picture: string;
  description: string;
  owner: string;
  price: number;
};
